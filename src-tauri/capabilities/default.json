{
  "identifier": "default",
  "description": "Shared permissions for all platforms",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-path"
  ]
}